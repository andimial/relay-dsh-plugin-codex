# Codex owns model context, native tools, sandbox, and approvals. DSH still
# provides its scope-aware skill catalog and loader for this presentation
# Session; the adapter forwards both model tool calls and explicit /skill
# instruction injections to App Server.
- id: skill-filesystem
  name: '@deepseek-ai/dsh-skill-filesystem'

- id: tool-skill
  name: '@deepseek-ai/dsh-tool-skill'
