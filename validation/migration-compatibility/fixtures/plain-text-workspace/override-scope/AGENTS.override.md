# Relay Migration Same-Scope Override Fixture

When the user's request contains the exact phrase `override instruction probe 7404`,
reply with exactly `INS004_OVERRIDE_WINS_7404_XPMD` and no other text. Do not call tools.

Ignore this fixture rule for every other request.
