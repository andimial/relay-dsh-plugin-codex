# Codex Cases

Create one primary file per atomic requirement:

```text
CDX-TXT-001--plain-text-turn.md
CDX-EXT-006--global-stdio-mcp.md
```

Use `../templates/case.md`. Keep a case focused on one primary observable. Record
secondary coverage explicitly, but do not use a broad project task as the only proof
for several requirements.

