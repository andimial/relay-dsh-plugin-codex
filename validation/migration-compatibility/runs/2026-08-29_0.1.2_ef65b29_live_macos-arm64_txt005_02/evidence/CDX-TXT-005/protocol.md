# CDX-TXT-005 Protocol Evidence

The focused protocol lane passed in the immediately preceding TXT-005 run at the
same plugin commit and environment (`tests 1`, `pass 1`, `fail 0`). It asserts
distinct `reasoning-delta` and `text-delta` values. The current run reuses that
immutable same-baseline evidence and concentrates on the corrected High-effort Web
lane.

