# CDX-EXT-014 Live Evidence

- Official default-discovery/schema contract, direct handler oracle, installed bytes,
  and direct Codex CLI control all validate the fixture and upstream runtime.
- DSH App Server skipped the plugin Hook under both default trust policy and the official
  vetted trust bypass.
- The authoritative target command ran and created the file that the Hook should have
  denied; no DSH Hook event or handler log exists.
- Isolation/bypass cleanup completed and the generated target file was removed.

Result: **fail** for the current Codex plugin integration path.
