# CDX-CFG-002 Project Server Log

- After the positive branch, the shared project-server log baseline was 136 lines,
  13,313 bytes, modification epoch `1787983987`.
- Its tail records one positive `tools/call` followed by exact tool/token values.
- After the complete negative branch it remained 136 lines, 13,313 bytes, and the
  same modification epoch.
- Therefore the sibling Session neither started nor called the project MCP server.
