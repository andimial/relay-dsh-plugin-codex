# CDX-TOOL-009 Live Evidence

The executed backend path yielded the first and last markers separately and in order,
but the browser captures missed the interval between those yields. See
`interim-observation.md`.

Result: **not-run (invalid timed capture)**; no product interpretation.
