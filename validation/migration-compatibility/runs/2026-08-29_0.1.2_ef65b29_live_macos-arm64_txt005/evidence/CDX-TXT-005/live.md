# CDX-TXT-005 Live Evidence

## Low-effort preflight

- A fresh Codex Session inherited `GPT-5.6-Sol` / `Low`.
- Sent the exact calculation/final-marker prompt once.
- One terminal `p` rendered exactly `CDX_REASON_FINAL_005_5017`.
- The turn completed in `7.3s`, the stop control disappeared, and no duplicate final
  paragraph appeared.
- No `Think` or other reasoning disclosure was present in the completed accessibility
  DOM.

This does not test rendering of a reasoning event because the Low-effort backend did
not emit one. Result for this preflight: **not-applicable**. A High-effort run is
required.
