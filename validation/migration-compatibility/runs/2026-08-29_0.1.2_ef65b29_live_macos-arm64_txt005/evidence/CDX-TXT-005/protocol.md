# CDX-TXT-005 Protocol Evidence

Focused result on 2026-08-29:

```text
tests 1
pass 1
fail 0
duration_ms 85.814667
focused_test_ms 3.822875
```

The named test independently asserts a `reasoning-delta` value of
`Checked the workspace.` and a `text-delta` value of `done`. This proves channel
separation at the adapter boundary; native Web presentation remains mandatory.

