# CDX-EXT-008 Live Evidence

- Direct Streamable HTTP initialize/notification/list/call passed.
- Isolated URL config was enabled; real user config stayed unchanged.
- Server log proves Codex transport requests and one exact business call after direct
  oracle traffic.
- Native Codex event proves exact HTTP server/tool provenance, input, text output, and
  structured transport.
- DSH displayed/persisted exact `HTTP_MCP_OK_8842_CWNS` and completed normally in
  17.1 seconds.
- Server/config digests remained stable.

Result: **pass**. Local Streamable HTTP MCP connects, executes, and delivers its result
through the current Codex plugin.
