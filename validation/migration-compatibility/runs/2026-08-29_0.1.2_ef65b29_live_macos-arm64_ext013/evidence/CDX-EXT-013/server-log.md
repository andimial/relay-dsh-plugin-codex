# Plugin MCP Server Log Evidence

- Product startup created server processes whose cwd and `import.meta.url` both point
  inside isolated installed cache version `0.1.0+codex.20260829052233`.
- The business process initialized, received `tools/list`, and received exactly one
  `tools/call` for `plugin_echo_1313` with token `PLUGIN_MCP_REQ_1313`.
- Exact authoritative business-call count: one.
- The separate direct-oracle process path points to the editable repository fixture,
  making source and installed execution unambiguous.
