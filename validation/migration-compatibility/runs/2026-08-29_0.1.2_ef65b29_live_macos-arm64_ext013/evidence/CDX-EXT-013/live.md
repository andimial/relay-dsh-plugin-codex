# CDX-EXT-013 Live Evidence

- Direct protocol oracle passed before install.
- Canonical plugin refresh/reinstall/Host restart completed in isolation.
- Server log proves execution from the versioned installed plugin cache.
- Native Codex event binds the exact MCP call and result to the exact installed
  `plugin_id`.
- DSH delivered the exact terminal marker once.

Result: **pass**.
