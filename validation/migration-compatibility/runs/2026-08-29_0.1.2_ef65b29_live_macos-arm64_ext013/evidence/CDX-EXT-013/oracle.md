# Plugin MCP Direct Oracle

- Direct STDIO JSON-RPC initialized protocol `2025-06-18` and server identity
  `relay-plugin-mcp-fixture` version `1.0.0`.
- `tools/list` returned exactly `plugin_echo_1313` with the intended schema/annotations.
- One direct `tools/call` with `PLUGIN_MCP_REQ_1313` returned exact text marker and
  structured marker/token/source fields.
- Direct log proves one initialize, notification, list, and business call from the
  editable fixture source before installation.
