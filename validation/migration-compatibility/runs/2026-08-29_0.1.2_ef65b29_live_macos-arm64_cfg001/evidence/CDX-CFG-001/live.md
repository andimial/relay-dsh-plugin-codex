# CDX-CFG-001 Live Evidence

- Without the user config table, the unique tool was absent from the real code-mode
  catalog and no fixture process existed.
- With only that user table added, the product launched the exact source, advertised
  the exact read-only schema, executed one exact call, retained both result forms, and
  delivered the final marker to DSH.
- Configuration and Host were restored afterward.

Result: **pass**.
