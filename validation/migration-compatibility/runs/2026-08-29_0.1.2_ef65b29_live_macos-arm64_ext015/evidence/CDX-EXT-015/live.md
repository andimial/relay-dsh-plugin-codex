# CDX-EXT-015 Live Evidence

- A fresh Codex-backed DSH Session was created from the intended fixture Workspace.
- The exact prompt required one DSH read and prohibited all alternatives.
- Codex selected the advertised `dsh.read` capability through unified execution and
  returned the exact first-line marker.
- Native rollout, DSH archive, browser presentation, and independent fixture bytes all
  agree.

Result: **pass**.
