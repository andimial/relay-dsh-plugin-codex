# Project MCP Server Log

Runtime log: `/private/tmp/relay-cdx-ext007-20260829-project-log.jsonl`.

- Two positive-Workspace processes started (pids `40220`, `40230`) for separate Host
  consumers; both initialized and listed tools.
- Exactly one `tools/call` occurred on pid `40230`, name `project_echo_7731`, exact token
  `PROJECT_INPUT_7731_HZKP`.
- No process start, initialize, list, or call was appended during either negative
  sibling-Workspace branch.

This independently proves positive project provenance and negative non-start.
